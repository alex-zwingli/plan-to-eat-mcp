node_modules/
dist/
*.tsbuildinfo

# Credentials and cached auth — never commit these.
.env
session.json

# Local-only tooling: deploy target, machine-specific notes, local CC config.
/deploy.sh
/.local/
/.claude/commands/
/.claude/settings.local.json
