node_modules/
dist/
*.tsbuildinfo

# Credentials and cached auth — never commit these.
.env
session.json

# Local-only tooling: machine-specific notes, local CC config.
/.local/
/.claude/commands/
/.claude/settings.local.json
